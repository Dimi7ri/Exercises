The purpose of this snippet is to check if the port 80 is available.
In Linux ports lower than 1024 are only available for root user only.

Uses Java Threads, Exceptions and ServerSocket to check if port 80 is available.

To execute via console:
- javac CheckPort80.java
- java CheckPort80
